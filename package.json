{
  "name": "greta-orto-site",
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "import-home-photos": "node scripts/import-home-photos.mjs",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.3.0",
    "@astrojs/mdx": "^5.0.4",
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.1.8",
    "sharp": "^0.34.0"
  },
  "devDependencies": {
    "svg-path-bounds": "^1.0.2",
    "wrangler": "^4.87.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
